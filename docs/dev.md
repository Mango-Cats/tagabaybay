# Development Notes

## On [C] mappings

## On unpredictable variant mappings
If `is_unpredictable_variant == true` and there is no mapping for the vowel
write it as greek equivalents instead to indicate its variance:
a -> α (alpha)
e -> ϵ (epsilon)
i -> ι (iota)
o -> θ (theta)
u -> μ (mu)