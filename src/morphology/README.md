# English Morphology
=============================

Dictionary-based morpheme segmentation with pattern matching for unknown words.

## Setup

```bash
python train_model.py
```

This creates `model.bin` with 85+ morpheme rules.

## Test

```bash
python test_model.py
```